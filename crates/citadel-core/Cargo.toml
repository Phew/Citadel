[package]
name = "citadel-core"
description = "Client core: MLS state, sync, local store (stub in M0)"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
citadel-proto = { workspace = true }

[dev-dependencies]
