# K3 status — M1 endgame session (2026-07-19 late / 2026-07-20)

Written for a fresh K3 instance with zero memory of prior sessions. Read
plans/PLAN.md, plans/AGENTS.md, plans/PLAN-KIMI-K3.md first (restate the six
Scope Discipline Rules verbatim), then this file. Earlier handoffs are in
this branch's git history.

## Environment facts (day-3 set still holds, with these corrections)

- Worktree `C:\Users\charge\Documents\GitHub\Citadel\citadel-k3`; work only
  on `k3/<task>` branches; `git fetch --prune` first; `gh` authenticated;
  no local Docker/Postgres; `python` not `python3`; `MSYS_NO_PATHCONV=1`
  for `git show <ref>:<path>`.
- **Rule 13 (plans/AGENTS.md, since PR #28): NO AI attribution signatures
  in commits.** Do not sign "K3 (kimi)" or anything like it.
- **Docs-only diffs (docs/, plans/, *.md) skip CI entirely BY DESIGN**
  (PR #26 path filters). Zero check-runs on a docs branch is correct, not
  an outage. Do NOT retrigger CI by force-amending open PR branches — if a
  run is genuinely owed and missing, escalate.
- **Base every PR on main** and note stack order in the body; never base
  on another PR's branch (GitHub auto-closed #24 when #23's branch was
  deleted).

## M1 state: COMPLETE pending final reviews

Main is at `f8e21f4`+ (check `git log origin/main` — it moves fast).
Everything K3 owns for M1 is merged or in final review:

- MERGED: #21 (confinement wiring + checkout v5), #23 (challenge/token,
  ADR-0003 §1–§3), #24 (KT persistence + read endpoints, ADR-0001 §4),
  #25 (registration + pool endpoints, ADR-0003 §4/§6), #29 (Opus:
  kt_appended_at on RegisterAccountResponse — my flag, issue 008), #30
  (issue 008 paper trail), #31 (harness AC scaffolding), #32 (ADR-0004
  ACCEPTED).
- **PR #33 `k3/m1-device-enrollment` @ cd9e601 — OPEN, Opus blocking
  review.** Device enrollment (ADR-0004) + all seven named Evidence tests
  + 12th canary probe + harness AC wired to the real 3 accounts × 2
  devices gate. Fully green: run 29712827217 (db-tests job 88259732748 —
  7 enroll_* tests; compose-smoke job 88259732770 — AC test ok; canary job
  88259732741 — 12 canaries CLEAN). Evidence excerpts posted as a PR
  comment. **Merging #33 closes the M1 exit AC.**

## Knowledge that cost CI cycles (don't relearn)

- Release containers carry ONLY the binary: migrations must be embedded
  (`sqlx::migrate!("./migrations")` + the `macros` feature), never
  resolved from CARGO_MANIFEST_DIR at runtime.
- db-tests share one job database. Tests touching globally-sequenced
  tables (kt_leaves.seq) MUST use per-test PostgreSQL schemas (CREATE
  SCHEMA + after_connect SET search_path; see kt_persistence.rs /
  enrollment.rs `fresh_pool`).
- proto's b64 helpers are STANDARD with padding; tokens are base64url
  no-pad (ADR-0003 §2) — don't hand-write base64 in tests.
- Services are crypto-confined INCLUDING dev-deps (ADR-0002 §4): client
  signatures in tests come from test-harness `testkeys::TestSigner`.
- Registration/enrollment store the credential as `tbs.signing_input() ||
  signature` (canonical signed bytes) in devices.credential.

## Open items for M2+

- Device transparency (per-device KT leaf) is a named residual gap,
  deferred by ADR-0004's acceptance; needs a proto change + ADR if charge
  wants it later.
- docs/protocol/auth.md §5 consistency endpoint is live (since #24); the
  doc line can be marked landed (Opus owns it).
- M8: rate limiting (registration-abuse risk is ACCEPTED in ADR-0003 §7);
  token-validation cache revisit; device revocation UX / recovery.
- PLAN.md §9 M2 is next: delivery-service message path, citadel-core
  OpenMLS, padding buckets. Not started.
