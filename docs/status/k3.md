# K3 status — end of M1 kickoff session (2026-07-17)

Written for a fresh K3 instance with zero memory of this session. Read
plans/PLAN.md, plans/AGENTS.md, plans/PLAN-KIMI-K3.md first (restate the six
Scope Discipline Rules verbatim), then this file, then the linked
docs/issues and docs/decisions.

## Environment facts (not written down anywhere else)

- **Your worktree:** `C:\Users\charge\Documents\GitHub\Citadel\citadel-k3`
  (registered git worktree of the main checkout). Work ONLY here, on
  `k3/<task>` branches. The primary checkout (`...\Citadel\Citadel`) is
  charge's. Opus works in `...\Citadel-opus`, Grok in `...\citadel-grok`.
- **No `gh` CLI, no Docker, no local PostgreSQL on this box.** You cannot
  open PRs or run stack/DB tests locally; CI is the proving ground (that is
  the PLAN.md §13 model). Ask charge to confirm CI results or install `gh`.
- Git identity is charge's; commit messages carry attribution via the
  `k3/` branch prefix and the message body. Pushes over HTTPS work
  (credential manager is set up).
- `LF will be replaced by CRLF` warnings on commit are harmless (Windows).
- Remote: `https://github.com/Phew/Citadel.git`. charge merges PRs on
  GitHub; M0 landed as PR #1.

## State of your six branches (all pushed, all in sync, stack in this order)

Each branches off the previous; charge merges in this order. PR
descriptions (milestone/flow/invariants/named tests per AGENTS.md rule 10)
are in each commit message.

1. `k3/m1-ci-hardening` (3903cf9) — CI hardening: action SHAs pinned,
   `permissions: contents: read`, concurrency cancel, timeouts, `--locked`,
   toolchain single-sourced from rust-toolchain.toml. Carries
   `docs/issues/003` (auth.md escalation) and `docs/backlog.md`.
   **Waiting on:** charge merge.
2. `k3/m1-harness-framework` (b70aed0) — test-harness `stack` (loud
   require_stack) + `client` (TestClient) modules; smoke test runs in the
   compose-smoke CI job via `--include-ignored`. **Waiting on:** merge;
   CI result of the compose-smoke job.
3. `k3/m1-canary-scan` (94a16af) — M1 exit requirement. `canary-scan`
   inject/verify binary (bodies/headers only, raw/hex/base64 encodings,
   control-canary self-test, coverage gates) + `canary-scan` CI job.
   **Waiting on:** merge; CI result of the canary-scan job (first real run
   — local box has no Docker/PG, so watch for teething issues: compose log
   capture, CITADEL_DATABASE_URL reachability).
4. `k3/m1-keypackage-pool` (7d3ce27) — M1 AC. migrations/0001 (accounts,
   devices, key_packages per PLAN §6), FOR UPDATE SKIP LOCKED store
   (all-or-nothing account fetch), property tests vs real PG16 + the
   `db-tests` CI job (postgres:16 service container).
   **Waiting on:** merge; CI result of db-tests job; **Opus blocking
   review** of the consumption path (PLAN-OPUS M1 task 3).
5. `k3/m1-kt-adr-review` (290f570) — design review of ADR-0001 →
   `docs/issues/004`, verdict approve-with-changes (F1 persistence schema
   vs PLAN §6 — both leaf AND STH tables needed; F2 log pubkey
   distribution unspecified; F3/F4 minor). Also renumbers your escalation
   to `docs/issues/003` (Opus holds issues 001/002).
   **Waiting on:** charge/Opus response to F1/F2 before ADR-0001 ACCEPTED.
6. `k3/m1-auth-params-adr` (dd21881) — ADR-0003 PROPOSED: auth-flow
   operational parameters (120s single-use challenges, 24h hashed opaque
   tokens, revocation via device join, pool batch cap 100, KT proof+head
   atomic response, 1–64B non-unique handles). **Waiting on:** Opus review
   + charge decision. This substitutes for the missing
   docs/protocol/auth.md.

## Tomorrow's queue (in order)

1. Check merge state: `git fetch origin --prune`; if main moved, rebase the
   remaining stack (`git rebase --onto` chains or restack from the
   oldest unmerged branch).
2. **deny.toml crypto-confinement bans** (accepted from Opus's
   docs/issues/002 into your lane): `[[bans.deny]]` for ed25519-dalek,
   sha2, sha1, md-5, ring, aes-gcm, chacha20poly1305, hmac, hkdf, rand,
   rand_core, getrandom with `wrappers = ["citadel-service-crypto",
   "kt-log"]`. Blocked until the facade crates exist on main (cargo-deny
   wrappers must resolve). Provide the required evidence: a test PR or
   local run showing CI fails when a service crate adds a direct crypto
   dep.
3. **auth-service endpoints + KT persistence** once ADR-0003 is ACCEPTED
   and opus/m1-proto is on main: F1 endpoints (register, enroll, publish,
   consuming fetch, challenge/verify), token middleware per ADR-0003,
   kt_leaves + kt_sth migrations (watch issue 004 F1 for the final schema),
   startup root-mismatch fatal check with test
   `startup_fails_on_tampered_leaf_bytes` (issue 004 F4). Extend canary
   injection points to the new endpoints (rejected-payload canaries only —
   never canaries in fields that are legitimately stored, e.g. valid
   handles). Every endpoint ships with its integration test in the same PR.
4. Then the M1 harness AC: register 3 accounts × 2 devices, verify KT
   inclusion proofs through the full stack.

## Open items you are waiting on

- **From charge:** merges (opus/m1-proto, then your stack); ADR-0003
  decision; ADR-0001 F1/F2 resolution; Opus issue 001 decision (Go oracle
  import — option A has you CI-wiring it); CI results for the new
  `db-tests` and `canary-scan` jobs (you can't see them without `gh`).
- **From Opus:** blocking review of the KeyPackage pool (PR-4); review of
  ADR-0003; the proto KT proof+head wrapper type if ADR-0003 §5 is
  accepted (Opus is sole proto merger — never edit citadel-proto yourself);
  docs/protocol/auth.md if charge rules a narrative spec is still needed
  beyond ADR-0003.

## Conventions established this session (follow them)

- Infra-needing tests are `#[ignore = "requires ...; CI <job> runs it"]`
  and run in a dedicated CI job with `--include-ignored`. Default
  `cargo test --workspace` must stay infra-free. Missing infra = loud
  failure, never a skip (§13, scope rule 6).
- Stack branches sequentially; keep each PR focused and ≲600 hand-written
  lines (Cargo.lock churn doesn't count but note it in the PR).
- Issue numbering: Opus 001/002, K3 003/004. ADR numbering: Opus 0001/0002,
  K3 0003. Next free: issue 005, ADR 0004.
- sqlx notes: runtime `Migrator::new(&Path)` (feature "migrate") — the
  `migrate!` macro is NOT used; bind `Vec<Vec<u8>>` as `bytea[]` via
  UNNEST; `to_jsonb(t)::text` for generic row dumps (bytea renders `\x`hex
  — the canary scan's hex encoding covers it).
- The canary scan must never place canaries in paths/query strings
  (TraceLayer logs URIs legitimately) or in fields that are supposed to be
  stored (valid handles) — bodies and headers of rejected/anonymous
  requests only.
