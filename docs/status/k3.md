# K3 status — session log, newest entry last (current through 2026-07-25)

For a fresh K3 instance with zero memory. Read `plans/PLAN.md`,
`plans/AGENTS.md`, `plans/PLAN-KIMI-K3.md` first, then this. You own backend
services (auth, directory, blobstore, delivery transport), harness/CI, and you
are the independent design reviewer of the **core lane's** ADRs. Never review
your own code; the core lane blocking-reviews your security-adjacent code.

**Roster note:** the core lane is held by **Sol** (GPT-5.6 Sol) since
2026-07-25; Claude Opus 4.8 held it through M1 and is off the roster. Entries
below written before that date name Opus; read those as "the core lane." The
authoritative roster is the table in `plans/AGENTS.md`, which logs every swap
under rule 12. This file is append-only: **scroll to the last section for
current state**, since earlier entries are historical and deliberately not
rewritten.

## SESSION 1 (2026-07-21) — M2 start

- M1 closed; M2 open. ADR-0005 (M2 DM delivery wire model) **ACCEPTED** with
  Amendment 1, merged to main in **PR #36** (`d2d9863`).
- My independent design review of ADR-0005: **approve**, posted on #36. Two
  non-blocking gaps I raised (groups-row lifecycle, submit-authorization
  circularity) were folded as **Amendment 1** — doc-only; proto contracts
  unchanged. I also added `subscribe_rejects_non_addressee` to the ADR's
  Evidence list (the §1 subscription check had no named test).
- Now building the delivery-transport half of M2 on branch
  **`k3/m2-delivery-service`** (based on main, not stacked): POST/GET
  `/v1/groups/{gid}/messages`, WS `/v1/gateway`, ciphertext-only schema,
  canary extension, and the named evidence tests (my half of F2/F4 is
  transport-level; the MLS-backed F2/F4 harness tests land once Opus's
  citadel-core M2 path exists).

Build spec: `docs/decisions/0005-m2-dm-delivery-wire-model.md` (final,
Amendment 1). Contracts: `crates/citadel-proto/src/delivery.rs` (frozen; Opus
is sole proto merger — do not touch).

## SESSION 1 close (2026-07-21) — delivery-service built, PR #39 up

**PR #39** (`k3/m2-delivery-service`, draft → ready once CI re-greens on
`70fd770`): the full M2 delivery transport. Commits `7a70b15`..`70fd770`.

- delivery-service is now lib+bin: `store.rs` (submit transaction:
  participant check BEFORE the lazy groups-row upsert so a rejected first
  submit never locks the gid; idempotency pre-check; `INSERT ... ON CONFLICT
  DO NOTHING`; `SELECT next_seq ... FOR UPDATE` + bump; insert with
  idempotency ON CONFLICT → no row means a concurrent replay raced in, so
  ROLLBACK — that rollback undoing the seq bump is what keeps seq gap-free),
  `auth.rs` (ADR-0003 §3 validate_token replicated against the shared DB,
  sha256 via the facade — the only crypto linked, rule 6), `server.rs`
  (POST/GET/gateway routes; fanout broadcast AFTER commit, only on Created),
  `gateway.rs` (push-then-mark welcome delivery = at-least-once; subscribe
  authz = same participant predicate; no send frame).
- Migration numbering starts at **0004** (auth owns 0001–0003 in the shared
  `_sqlx_migrations`); BOTH migrators run `ignore_missing` — I patched
  auth-service's store.rs for this. Remember this for every future service
  that migrates the `citadel` DB.
- Canary: two new probes (authed → dies in `validate()`; unauthed → 401),
  both rejected before the store layer. dbscan enumerates tables
  automatically — no scanner change needed for new tables.
- Verified in CI logs (not just green checks): db-tests ran
  `submit_is_idempotent_and_seq_monotonic`,
  `seq_gap_free_proptest_random_concurrency`,
  `submit_rejects_non_participant`, `fetch_pages_are_bounded_and_complete`,
  `welcome_deliveries_track_recipients_and_delivery` on real PG16;
  compose-smoke ran `f2_gateway_welcome_delivery` +
  `subscribe_rejects_non_addressee`; canary-scan clean.
- Review nits I fixed myself post-implementation (`70fd770`): version
  rejection → `unsupported_version` taxonomy (INV-5), `?after=` clamped to
  i64::MAX instead of wrapping negative.
- NOT here (by design): full MLS-backed `f2_three_client_dm_creation` /
  `f4_send_receive_roundtrip` (need Opus's citadel-core M2 path), GET-path
  participant check (ADR-0005 pins it for submit+subscribe only, bearer-only
  for reads), one-commit-per-epoch (M3), franking (M6), rate limits (M8).

## SESSION 2 (2026-07-22) — blocking-review fixes on #39

Blocking review landed three findings; fixed two, held one:

1. **Founding race (fixed, `c140dba`)** — founder status is now atomic with
   groups-row creation: for a Welcome the upsert IS the founder test
   (`rows_affected == 1` → founder; `0` → fall through to
   `is_participant_in`, which a racing non-member fails after Postgres
   unblocks it on the winner's committed row). Proof:
   `founding_race_admits_only_winning_founder` (16-round barrier race, real
   PG16 in CI).
2. **Welcome at-least-once wrt consumption (fixed, `c140dba`)** — the
   gateway now marks NOTHING at push time; a welcome is marked delivered
   only by the client's post-verification Subscribe naming its group (F2's
   consumption signal). Re-pushed on every connect until then.
   Proof: `welcome_redelivered_until_subscribe` (disconnect-before-consume →
   redelivery; subscribe → no further re-push).
3. **Migration architecture (HELD)** — ADR-0006 (Sol) decides one canonical
   migrator vs governed partial migrators. The `ignore_missing` + 0004
   numbering setup stays as-is until charge accepts that ADR; #39 stays
   DRAFT until then and merges after.

Note: `migrations/0004` still carries the old push-then-mark comment —
deliberately not edited (changing an applied migration's text changes its
sqlx checksum and breaks persistent dev DBs); authoritative behavior is in
`gateway.rs`/`store.rs` comments. Docker is unavailable on this machine, so
db/stack verification runs in CI (db-tests + compose-smoke jobs), never
locally.

## SESSION 3 (2026-07-24) — ADR-0006 CORE landed on #39; READY at 84dfa36

ADR-0006 (canonical migrations, Sol) is **ACCEPTED** on main with phased
scope; **Amendment 1 ACCEPTED 2026-07-23**. #39 now bundles the delivery
transport AND the full ADR-0006 CORE (`crates/citadel-migrations` with
corpus 0001–0004 moved byte-identically, canonical runner + `citadel-migrate`
binary, manifest, `ci/check_migrations.py`, compose migration-job gate,
`just migrate`; auth/delivery no longer self-migrate — the session-2
`ignore_missing` setup is GONE, both overrides removed per ADR-0006 §5).

Fixes this session, in order:

1. **SQLSTATE 42501 (run 29983887580 root cause)** — the preflight pinned
   `search_path = pg_catalog, public, pg_temp`; PostgreSQL creates
   unqualified objects in the FIRST schema of search_path, so sqlx's
   unqualified `CREATE TABLE _sqlx_migrations` targeted the system catalog.
2. **enrollment.rs** still used the pre-ADR-0006 per-test SCHEMA pattern
   (after_connect search_path pin) — the canonical runner migrates `public`,
   queries ran in an empty schema → 500s. Converted to the throwaway
   per-test-DATABASE pattern (same as registration_pool/kt_persistence).
3. **Amendment 1 correction** — my first fix used `public, pg_catalog,
   pg_temp`, which Amendment 1 explicitly REJECTS (public ahead of the
   catalog lets public objects shadow built-ins). Final value:
   **`public, pg_temp`** — pg_catalog unnamed (implicit-first lookup),
   public first explicit (creation target), pg_temp last (hardening).
   Evidence: `canonical_migrations_create_public_history_with_catalog_and_temp_precedence`
   (all three pairwise lookup edges, relations AND types, real PG16).
   ADR-0006 §1 body text on main was stale vs Amendment 1; advisor
   reconciled it in PR #44 (main `8a07668`).
4. **Sol review finding 1: preflight TOCTOU** — preflight ran before
   run_direct acquired sqlx's advisory lock. `migrate_with_bounds` now takes
   sqlx's own lock id (`0x3d32ad9e * CRC32(db name)`, derived in-crate;
   test-side CRC stays the independent oracle) BEFORE preflight; session
   advisory locks are re-entrant so run_direct nests inside the hold.
   Evidence: `canonical_migration_preflight_runs_under_migration_lock`
   (lock held + planted bad history → second runner dies on the lock wait,
   never Preflight; after release → Preflight).
5. **Sol review finding 2: checker gaps** — `ci/check_migrations.py` now
   pins the runner's SEARCH_PATH const to exactly `public, pg_temp` and
   flags any other inline search_path list; locking ban also matches
   `.set_locking(false)`; new injected probes (both rejected orderings,
   wrong-schema, inline literal, locking field, locking builder). Verified
   Sol's exact 0-violation mutations now each produce exactly one violation
   from the intended rule.
6. Sol's third finding (a "no-comments rule") was REJECTED by the advisor —
   AGENTS.md rule 9 encourages comments; none stripped.

**State: PR #39 is READY (non-draft) at `84dfa36`, run 30068825207 fully
green** — all evidence confirmed in the logs (12 canonical PG16 tests, 7
enroll_*, the 5 delivery evidence tests, canary clean with controls proven,
`migration-check: OK` after all 12 self-test probes fired). Waiting on
**Sol's re-review of the two fixes**, then charge merges.

**Next action on resume: blocking security review of PR #38** (citadel-core,
Sol's inherited code — the plaintext boundary, never independently
reviewed): INV-4 KT-verified join, INV-2 key handling, padding, INV-10.
ADR-0006 follow-ups A–D (role isolation/permissions evidence, service
minimum-schema declarations, fail-closed risk classification, rollback
check-only evidence) remain mine and are NOT started.

## SESSION 4 (2026-07-24) — #38 blocking review posted; #39 Sol re-review fixes

**PR #38 blocking review: K3 review — CHANGES** (comment posted). Two
blocking findings: (1) INV-4 enforced join-side only — `add_members`
accepts KeyPackages with no KT verification, exactly the path
`adversarial_ds_swapped_keypackage_rejected` (ADR-0005 §5, initiator-side)
attacks; the join-side check never runs for the victim since the Welcome
is encrypted to the attacker's KeyPackage key. (2) `receive()` drops
`StagedCommitMessage` without merging, so peer self-update commits never
advance the group — load-bearing for the M2 FS/PCS exit AC, mislabeled
"M3" in a comment. Padding, frozen proto contracts, join-side KT gate,
and the INV-2 network boundary all verified solid. CI logs opened line by
line: 13 citadel-core tests ran, cargo-deny/audit ran (suppression block
came from main via #41, not silenced in the PR), canary clean.

**#39 Sol re-review: migration lock/settings leak — FIXED.** Sol's
finding verified against sqlx-core 0.8.6 source before touching code:
`run_direct` unlocks only on its success path (every `?` after
`conn.lock()` returns with the acquisition held — Dirty, VersionMismatch,
ensure_migrations_table failure, failing migration, tokio backstop
cancellation), and `return_to_pool` (pool/connection.rs) puts a dropped
PoolConnection back WITHOUT a session reset, so the leaked hold — plus
the SET search_path/lock_timeout/statement_timeout on ALL paths — rode
the pooled connection to its next tenant. Fix in `migrate_with_bounds`
(now `migrate_inner`): unconditional exit cleanup —
`pg_advisory_unlock_all()` + `RESET lock_timeout/statement_timeout/
search_path` on success and error alike; on the tokio backstop the
mid-flight connection is CLOSED instead (no SQL is trustworthy on it; the
dying backend releases everything; the server-side bounds kill any
orphaned statement). A cleanup failure also closes rather than returning
unknown state. `locking = false` NOT touched — ci/check_migrations.py's
ban stands and is correct.

Evidence (all new tests in crates/citadel-migrations/tests/canonical.rs,
real PG16, each fails against the pre-fix runner):
- `canonical_migration_error_path_releases_lock_and_settings` — sabotaged
  0001 (conflicting table) fails under the lock; then zero advisory locks
  in pg_locks, the failed pooled session shows DEFAULT settings, and a
  follow-up migrate with the failed connection pinned out of the pool
  (forcing a DIFFERENT connection) succeeds instead of blocking to
  lock_timeout.
- `canonical_migration_backstop_cancellation_releases_lock` — pending
  0004 blocked mid-apply (SHARE lock on the history table passes the
  preflight's ACCESS SHARE reads but blocks run_direct's INSERT) until a
  2s tokio backstop fires; asserts MigrateError::Timeout, polls pg_locks
  to zero after the cancelled backend dies, and a subsequent migrate
  rolls 0004 to head.
- `canonical_migration_success_leaves_no_session_state` — pool(1) so the
  migrator's session is the one interrogated: after a clean apply the
  SHOWs equal the pristine-session defaults and no advisory lock
  survives.

Other two review items closed with no work: ci/check_migrations.py fix
PASSED Sol's re-review; the no-comments finding was rejected under
AGENTS.md rule 9. Status file lands in this same PR per rule 2 hygiene.

**Next:** Sol re-reviews the delta; on #38 + #39 merge, harness side of
the M2 exit AC: F2 + F4 across 3 clients on the live stack,
no-plaintext scan on delivery tables, FS + PCS, and
adversarial_ds_swapped_keypackage_rejected. (Note: the PCS/FS harness
tests depend on #38's staged-commit fix landing.)

## SESSION 5 (2026-07-25) — M2 exit AC harness: F2/F4 ×3, canary, PCS, adversarial DS

Both M2 build PRs merged: #39 at 33fcfe9, #47 (Sol's citadel-core respin,
both blocking findings fixed: initiator-side KT verification in
`add_members` with leaf-key binding, staged-commit merge in `receive`)
at 9a74d94. Branch `k3/m2-exit-ac` carries the harness side of the M2
exit AC.

New harness surface (`crates/test-harness`):
- `src/dm.rs` — a REAL test client: live registration + challenge auth,
  citadel-core `DeviceIdentity`/`DmGroup` for all group ops, KeyPackage
  publish + consuming fetch with MLS validation, REST submit, WS gateway,
  and `LiveKtVerifier` — a KT verifier that attests ONLY what it
  cryptographically verified against the live log (inclusion proof +
  signed head under the pinned anchor; the async/sync split is documented
  in the module: attest() does the I/O, the sync trait reads only
  verified facts).
- `src/dishonest.rs` — a genuinely dishonest DS: a live HTTP proxy that
  forwards the KeyPackage fetch to the real auth-service and rewrites the
  response to serve swapped packages. Not a stubbed verifier.
- `tests/m2_dm.rs` — the five evidence tests, all `#[ignore]` +
  `require_stack`, run by compose-smoke:
  - `f2_three_client_dm_creation` — 3 clients, KT-verified join,
    membership/epoch convergence.
  - `f4_send_receive_roundtrip` — fanout AND `GET ?after=` sync catch-up,
    bidirectional, seq/sender stamps asserted. (One test-authoring lesson:
    a subscribed sender's own submissions fan back to it; fanout reads
    filter the reader's device or OpenMLS errors CannotDecryptOwnMessage.)
  - `no_plaintext_scan_delivery_tables` — canary plaintext through the
    real F4 send path; zero hits in every public table (dbscan), none in
    the wire envelope or MLS ciphertext in any encoding, with non-vacuous
    row evidence (group_messages + welcome_deliveries counted).
  - `pcs_recover_after_update` — self-update commit via prepare → submit
    → confirm; peers merge over fanout to the proposed epoch; post-update
    traffic round-trips both directions.
  - `adversarial_ds_swapped_keypackage_rejected` — swapped package claims
    the VICTIM's account with attacker keys (the strong variant, not a
    random account); the proxy serves it on the real fetch path; the
    initiator rejects with MemberRejected(NotKtAttested) before any
    commit/Welcome exists; zero server-side rows for the aborted group;
    the live log itself refuses attest() for the attacker's key; honest
    control fetch succeeds first.
- `canary-scan inject` now also sends 2 canaries as REAL DM plaintext
  through the full F2+F4 path, so the CI canary job scans
  group_messages/welcome_deliveries/delivery logs for actual content
  (verified locally: 16 canaries, clean verdict, controls proven).

NOT done, deliberately: `device_compromise_past_messages_unreadable_fs`.
MLS state is in-memory; an FS test without persisted state would assert
that dropping an object loses the object — passes while proving nothing.
It lands with Sol's encrypted local store.

Scope discipline held: no changes to citadel-core, delivery-service, or
any service crate — harness, tests, and the canary binary only.

## SESSION 6 (2026-07-25) — M2 exit AC merged; #47 delta review; shutdown state

**M2 exit-AC harness MERGED at `295d829` (PR #49).** The five tests now
execute on every push to main via compose-smoke, not just on the PR:
`f2_three_client_dm_creation`, `f4_send_receive_roundtrip`,
`no_plaintext_scan_delivery_tables`, `pcs_recover_after_update`,
`adversarial_ds_swapped_keypackage_rejected` (the dishonest-DS proxy
rewrites the real KeyPackage fetch; the swapped package claims the
victim's account with attacker keys; honest control accepted first).
`canary-scan inject` sends real DM plaintext through the full F2+F4
path, so INV-1 is scanned against actual message content in
`group_messages` / `welcome_deliveries` / delivery logs on every push.

**#47 delta re-review: DONE, no findings.** Reviewed `7f2853f..9a74d94`
post-merge (the merge preceded the delta re-review window). Both
blocking findings verified fixed — initiator-side KT verification in
`add_members` (stronger than asked: also binds the MLS leaf key to the
credential's device_pubkey) and staged-commit merge in `receive` with
update-path KT verification. All three notes addressed (zeroization,
key-consistency at construction, no panic in new_key_package). Verdict
recorded as a comment on #47.

**`device_compromise_past_messages_unreadable_fs` is DELIBERATELY
UNWRITTEN — do not "fix" this.** MLS state is currently in-memory only.
An FS test against in-memory state can only assert that dropping an
object loses the object: it passes while proving nothing, and a green
but vacuous FS test is worse than a missing one because it looks like
evidence. The real test needs persisted client state to capture and
wipe, so it lands with Sol's encrypted local store (ADR-0007 track).
A fresh instance seeing the gap in the AC list must not close it with a
placeholder.

**Migration lock-cleanup fix (`8eaa6c0`, merged in #39) — the design
point worth preserving.** Sol's leak finding and the "release on the way
out" directive were right for every path EXCEPT the tokio backstop. On
cancellation the run future is DROPPED mid-flight; a statement may still
be executing server-side, so cleanup SQL on that connection queues
behind the orphaned statement on untrustworthy protocol state (measured:
a ~28s stall into a desync error). Following the directive literally
would have hung the runner. The fix therefore closes the connection on
the cancellation path instead — the dying backend releases all session
state — and runs `pg_advisory_unlock_all()` + session RESETs only where
the connection is known-clean (success and ordinary error paths). All
three evidence tests were mutation-checked to fail against the pre-fix
behavior for the intended reason.

**Next task on resume: independent design review of ADR-0007** (local
encrypted client store; on main, PROPOSED; Sol authored, unavailable to
defend). The load-bearing question: Alternative 2 rejects the stock
bundled SQLCipher 4.5.7 as "not 4.17.0 with current upstream SQLite
fixes" — a freshness preference, not a named advisory, and everything
expensive in the ADR (libsqlite3-sys patch, vendored OpenSSL with pinned
Configure transcripts, pinned NASM, three-OS amalgamation byte-comparison,
SBOM, OSV scanning) rests on that line. The review must answer: does a
NAMED advisory affecting 4.5.7's embedded SQLite in this usage exist?
If no, propose staging (M2 ships the store on the stock bundle; the
reproducibility program becomes its own ADR). Verdict goes as a labelled
comment on the ADR PR/issue; charge accepts or rejects.

## SESSION 7 (2026-07-26) — ADR-0007 design review posted: K3 review — CHANGES

**Independent design review of ADR-0007 delivered as
`docs/issues/009-adr-0007-store-design-review.md` (this PR).** Sol authored
the ADR and is out of quota; reviewed on the merits against source. Two
narrow required amendments; everything else approved as written.

**The load-bearing question, answered from primary sources, not
assertion.** Named CVEs against 4.5.7's embedded SQLite DO exist (verified:
libsqlite3-sys 0.30.1's bundle reports SQLITE_VERSION "3.45.3"; sqlite.org
CVE table fetched 2026-07-26 lists CVE-2025-3277, CVE-2025-6965,
CVE-2025-7709, CVE-2025-29088, CVE-2026-11822/11824 fixed after 3.45.3) —
but NONE is applicable in this usage. Every one needs attacker-controlled
SQL, an attacker-crafted database file, FTS5, DEFENSIVE=off, or app-side
C-API misuse; the ADR's own design forecloses each precondition (bound
parameters only, page HMAC, FTS5 omitted, DEFENSIVE=ON). cargo-audit (my
lane) over the ADR's exact pinned graph: zero advisories on any introduced
crate; the only hits were the pre-existing hpke-rs/libcrux chain (probe
artifact — main's lockfile pins the fixed hpke-rs 0.6.1).

**F1 (required): stage the overlay.** "Not 4.17.0 with current upstream
fixes" is a freshness preference and fails the ADR's own gate ("a relevant
advisory blocks this choice"). Proposed: M2 ships on the stock
`bundled-sqlcipher-vendored-openssl` bundle (4.5.7); the overlay +
reproducibility program (Configure transcripts, NASM pin, three-OS
amalgamation byte-compare, SBOM, OSV) becomes its own ADR; a lightweight
native-manifest + OSV scan of the stock bundle rides with the store build
as the tripwire. Staging forces two named adjustments: `PRAGMA
cipher_status` was added in SQLCipher 4.12.0 and does not exist on 4.5.7
(substitute the already-mandatory schema-access probe), and the
EXTRA_INIT/SHUTDOWN flags are mandatory only from 4.7.0 (re-derive the flag
set; `cipher_version` reads back 4.5.7).

**F2 (required): the ADR never names its deny.toml collision.**
`bundled-sqlcipher-vendored-openssl` = `["bundled-sqlcipher",
"openssl-sys/vendored"]`; the graph gains libsqlite3-sys → openssl-sys
0.9.117, and deny.toml bans openssl-sys graph-wide. Reproduced with the
repo's own config (cargo-deny 0.20.2): `error[banned]: crate 'openssl-sys
= 0.9.117' is explicitly banned`. Audit runs first and passes; deny is
where it fails, on either path (Windows has no system OpenSSL, so vendored
is effectively required regardless). Proven one-line fix: add
`wrappers = ["libsqlite3-sys"]` with a reason to the ban — re-ran
`cargo deny check bans` over the full ADR surface: bans ok. The ADR must
name this amendment because both configs are load-bearing.

**Verified strong (approved as written):** the FS test's demanded error
chain reproduced against openmls 0.8.1 source (PrivateMessage path →
UnableToDecrypt → SecretTreeError(TooDistantInThePast); the PublicMessage
path surfaces NoPastEpochData instead, so the ADR is right to demand an
application ciphertext); the shared-transaction type argument reproduces
(rusqlite Transaction derefs to Connection; provider bound is
`Borrow<Connection>`); keyring 3.6.3 really does hard-code
CRED_PERSIST_ENTERPRISE (windows.rs:246); one rusqlite/one libsqlite3-sys
resolve; openssl-src resolves exactly 300.6.1+3.6.3; max_past_epochs
default is already 0 in 0.8.1 (the pin is drift protection — non-blocking
doc note); PCS oracle pins resolve as dev-deps with zero advisories.

**Structural flag recorded for charge (not decided by me):** §6's narrowing
of PLAN §9 M2 "past messages unreadable" to a persisted-state boundary is
an acceptance-criterion change reserved to charge (AGENTS.md) and must be a
separate conscious decision, not inherited via ADR acceptance. Technically
sound (FS is a key-material property; Signal parity); advisor endorses; I
concur technically.

**Scope held:** no store implementation, no ADR-0006 follow-ups A–D, no FS
test written. Probe crate lived outside the repo and is deleted. Next on
the advisor queue after charge's ADR-0007 decisions: whatever the accepted
ADR scopes for my lane (FS test after the store lands), then A–D.
